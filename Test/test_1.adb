with Ada.text_io, Ada.Integer_text_io;
use Ada.text_io, Ada.Integer_text_io;

procedure test_1 is

sum, temp : Integer;

begin

sum := 1;
temp := (8 mod 3);

Put("The number is");
put(temp);

end test_1;
